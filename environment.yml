name: elsa-robotics-challenge
channels:
  - pytorch
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - flwr[simulation]>=1.15.1
      - flwr-datasets[vision]>=0.5.0
      - torch==2.10.0
      - torchvision==0.25.0
      - torchaudio==2.10.0
      - matplotlib
      - wandb
      - tqdm
      - moviepy==1.0.3
      - opencv-python-headless
      - hydra-core
      - omegaconf
      - ray
